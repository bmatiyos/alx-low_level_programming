#include "main.h"
/**
 * _abs - compits the obsolute of an integer
 * @n: integer for which to grt an absolute value
 * Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}
